{
  "private": true,
  "scripts": {
    "dev": "miniflare -o ROOM=Room -o LOBBY=Lobby --modules --watch",
    "build": "cfw build worker --single --no-minify",
    "types": "tsc --noEmit"
  },
  "dependencies": {
    "dog": "workspace:*",
    "worktop": "0.7.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "2.2.2",
    "cfw": "0.2.0",
    "miniflare": "1.4.0",
    "typescript": "4.4.2"
  }
}
