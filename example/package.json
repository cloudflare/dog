{
  "private": true,
  "scripts": {
    "dev": "miniflare -o ROOM=Room -o LOBBY=Lobby --modules --watch",
    "build": "cfw build worker --single",
    "types": "tsc --noEmit"
  },
  "dependencies": {
    "dog": "file:../",
    "worktop": "0.7.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "2.2.2",
    "cfw": "0.1.3",
    "miniflare": "1.3.3"
  }
}
